# 🚚 CargoTracker

> Современная система учёта грузоперевозок с веб-интерфейсом

## 📋 Описание проекта

CargoTracker - это современное приложение для управления грузоперевозками автомобильным транспортом с полнофункциональным веб-интерфейсом и облачным хранилищем данных.

### ✨ Основные возможности

- 📦 **Управление грузоперевозками** - создание, редактирование, отслеживание рейсов
- 👨‍✈️ **База водителей** - управление персоналом
- 🚛 **Учёт транспорта** - регистрация и мониторинг транспортных средств
- 📊 **Статистика и отчёты** - аналитика по перевозкам
- ☁️ **Облачное хранилище** - данные хранятся в Supabase PostgreSQL
- 🔄 **Синхронизация в реальном времени** - мгновенное обновление данных

## 🏗️ Технологический стек

- **Frontend**: Vue.js 3 + Vite + Bootstrap 5
- **Backend**: Node.js + Express
- **База данных**: Supabase PostgreSQL
- **State Management**: Pinia
- **Облачная интеграция**: Yandex Cloud Functions (опционально)

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 16+
- npm или yarn

### Установка и запуск

```bash
# 1. Установите зависимости
npm install

# 2. Запустите приложение (backend + frontend одновременно)
npm run dev
```

Приложение будет доступно по адресу:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3001

### Альтернативный запуск

```bash
# Запустить только backend
npm run server

# Запустить только frontend (в другом терминале)
npm run client
```

## 📁 Структура проекта

```
cargotracker-local/
├── public/              # Статические файлы
│   ├── index.html      # HTML шаблон
│   └── sw.js           # Service Worker
├── src/                # Исходный код frontend
│   ├── components/     # Vue компоненты
│   │   ├── Home.vue
│   │   ├── Shipments.vue
│   │   ├── Drivers.vue
│   │   ├── Vehicles.vue
│   │   └── Statistics.vue
│   ├── stores/         # Pinia stores
│   │   └── shipments.js
│   ├── api/           # API клиенты
│   │   └── client.js
│   ├── App.vue        # Главный компонент
│   ├── main.js        # Точка входа
│   ├── router.js      # Vue Router
│   └── style.css      # Глобальные стили
├── server/            # Backend сервер
│   └── supabase-server.js    # Express API сервер
├── cloud-functions/   # Yandex Cloud Functions (опционально)
│   └── supabase-function.js  # Облачная функция
├── config/            # Конфигурация
│   └── local-config.js
├── .env.example       # Пример настроек
├── package.json       # Зависимости
├── vite.config.js     # Конфигурация Vite
└── README.md          # Документация
```

## 🔧 База данных

Приложение использует **Supabase PostgreSQL** в качестве основного хранилища данных. База данных уже настроена и включает:

### Таблицы
- **shipments** - грузоперевозки (id, origin, destination, driver, vehicle, cargo, weight, status, created_at, updated_at)
- **drivers** - водители (id, name, phone, license, status, created_at, updated_at)
- **vehicles** - транспортные средства (id, number, brand, model, type, capacity, year, status, created_at, updated_at)

### Тестовые данные
База включает примеры:
- 2 водителя (Иванов И.И., Петров П.П.)
- 2 транспортных средства (КАМАЗ, MAN)

### Проверка работы
```bash
# Проверка здоровья API
curl http://localhost:3001/api/health

# Получить список перевозок
curl http://localhost:3001/api/shipments

# Получить водителей
curl http://localhost:3001/api/drivers

# Получить транспорт
curl http://localhost:3001/api/vehicles
```

## 📖 Использование

### Управление грузоперевозками

1. Перейдите в раздел "Грузоперевозки"
2. Нажмите "Создать перевозку"
3. Заполните форму с данными о рейсе
4. Сохраните - данные автоматически синхронизируются с облаком

### API Endpoints

**Грузоперевозки:**
```
GET    /api/shipments           - Список перевозок
POST   /api/shipments           - Создать перевозку
PUT    /api/shipments/:id       - Обновить перевозку
DELETE /api/shipments/:id       - Удалить перевозку
```

**Водители:**
```
GET    /api/drivers             - Список водителей
POST   /api/drivers             - Добавить водителя
PUT    /api/drivers/:id         - Обновить данные водителя
DELETE /api/drivers/:id          - Удалить водителя
```

**Транспорт:**
```
GET    /api/vehicles            - Список транспорта
POST   /api/vehicles            - Добавить транспорт
PUT    /api/vehicles/:id        - Обновить транспорт
DELETE /api/vehicles/:id         - Удалить транспорт
```

**Система:**
```
GET    /api/health              - Состояние системы
GET    /api/statistics          - Статистика
GET    /api/sync/status         - Статус синхронизации
```

## 🛠️ Разработка

### Доступные команды

```bash
npm run dev         # Запуск в режиме разработки (backend + frontend)
npm run build       # Сборка для production
npm run preview     # Предпросмотр production сборки
npm run server      # Запустить только backend сервер
npm run client      # Запустить только frontend
```

### Технологический стек

**Frontend:**
- Vue.js 3 - прогрессивный JavaScript фреймворк
- Vite - быстрый build tool
- Pinia - state management
- Vue Router - маршрутизация
- Bootstrap 5 - UI framework
- Bootstrap Icons - иконки

**Backend:**
- Express.js - веб-фреймворк
- Supabase - облачная база данных PostgreSQL
- @supabase/supabase-js - клиент для работы с Supabase
- Helmet.js - защита Express
- CORS - настроен для безопасности

## 🔐 Безопасность

- 🛡️ Helmet.js для защиты Express
- 🔒 Row Level Security (RLS) в Supabase
- 🚫 CORS настроен для защиты API
- 📝 Валидация данных на всех уровнях

## 🐛 Отладка

### Логи сервера
Сервер выводит подробные логи в консоль:
```bash
npm run server
```

### Инструменты разработчика
- Vue DevTools для отладки компонентов
- Network tab для мониторинга запросов
- Console для ошибок JavaScript

## 📈 Производительность

- ⚡ Vite обеспечивает мгновенную горячую перезагрузку
- 💾 Supabase обеспечивает быстрый доступ к данным
- 🔄 Service Worker кэширует статические ресурсы
- 📦 Code splitting для оптимизации загрузки
- 🚀 Оптимизированные запросы к базе данных

## 📚 Документация

- **DEPLOYMENT_GUIDE.md** - Полное руководство по развёртыванию приложения
  - Инструкции по локальной разработке
  - Настройка Yandex Cloud Functions
  - Развёртывание в production
  - Troubleshooting и оптимизация

## 🌐 Развёртывание в облаке

Приложение может быть развёрнуто с использованием Yandex Cloud Functions для масштабируемости и отказоустойчивости. См. **DEPLOYMENT_GUIDE.md** для подробных инструкций по:

1. Настройке Yandex Cloud
2. Созданию и развёртыванию Cloud Function
3. Настройке окружения
4. Мониторингу и логированию

## 🤝 Поддержка

Для получения помощи:
1. Проверьте логи сервера: `npm run server`
2. Проверьте консоль браузера на наличие ошибок
3. Убедитесь, что Supabase доступен
4. См. раздел Troubleshooting в DEPLOYMENT_GUIDE.md
